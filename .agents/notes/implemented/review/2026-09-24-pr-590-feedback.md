# PR 590 review follow-up

## Scope

Reviewed the inline threads, review summaries, and PR comments against
`efc62f17`. The user's intervening push contains the same YAML fix, `8158b198`;
keep that commit and apply the remaining fixes without duplicating it.

## Decisions

| Finding                                          | Resolution                                                                                          |
| ------------------------------------------------ | --------------------------------------------------------------------------------------------------- |
| Invalid labeler criteria, thread 4089982350      | `8158b198` quotes all 14 keys.                                                                      |
| Mutable labeler action, thread 4089982339        | `1dadecc2` pins the verified v0.3.1 commit.                                                         |
| Timestamp ties, review 5300000064                | `c81bcd58` orders by ID after creation time in both lookups.                                        |
| Session fixture ignores model, review 5300000064 | `b89c92c8` shares model filtering between both fixtures.                                            |
| Undocumented labeler secret, review 5299877857   | `516297e2` documents provisioning, rotation, and failure behavior.                                  |
| Duplicate model colors, thread 4089982368        | `5a5381f6` gives Grok a distinct green.                                                             |
| Error table formatting, thread 4090626494        | `d32d41a3` applies the formatter.                                                                   |
| 404 client compatibility, review 5300665088      | `083bd69c` documents handling for clients using `model_not_found`.                                  |
| Style guard and billing, thread 4087453691       | Retain the existing decision in reply 4090013089. Reported total tokens feed credit reconciliation. |

The YAML, action pin, and color findings also occur in review 5299877857.
Each defect has one fix; no review source was omitted. Repository-secret
metadata confirms `TYPESAFE_API_KEY` is configured. No secret value was read.

## Boundaries

Preserve the approved 404 behavior, oldest-row precedence, and provider IDs.
Keep the user-added labeler in this PR and retain visible failures. Featured
voices and free-tier pricing are decided in the
[catalog note](../tts/2026-09-23-gemini-38-catalog.md#featured-voices-and-pricing).
A display-name schema change, generator scope, and other explicitly deferred
choices remain as documented in PR comments 5807826621 and 5807826857.

The release-only changelog remains unchanged. The release announcement should
call out the 404 contract; client guidance is in the public error-code reference.
No database, R2, or customer-message operations are part of this review round.

## Verification

- Parsed the workflow and its embedded criteria YAML; all 14 criteria are strings.
- Verified that upstream `v0` and the annotated `v0.3.1` tag resolve to the pinned SHA.
- Tie-break regressions fail for both query helpers without the fix, then pass.
- The session fixture regression fails without model filtering, then passes.
- Full suite: 1,322 web tests and 63 script tests passed; 19 existing web skips.
- Selector tests passed after the color change.
- `pnpm fixall`, `pnpm type-check`, MDX formatting, and `git diff --check` passed.
